set(CMAKE_SYSTEM_PREFIX_PATH /usr/local)
#set this variable, so that find_package(), find_program(), find_library(), find_path() and find_file() will search the prefix for other software. 


set(CMAKE_INSTALL_PREFIX /selflibs)
#set this variable, so that tell where the installation will locate

